# tenv installation

The `tenv` role is applied to the `laptop` inventory group.

- Arch Linux installs the `tenv-bin` AUR package.
- Ubuntu installs `tenv` from the signed tofuutils Cloudsmith APT repository.

Other distributions are rejected explicitly by the role.
