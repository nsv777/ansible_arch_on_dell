---
- name: Install console utilities
  aur:
    name:
      - htop
      - mc
      - lshw
      - docker
      - docker-compose
      - man-db
      - nmap
      - ntfs-3g
      - powertop
      - python2
      - rsync
      - samba
      - wget
      - xz
  become: yes
  become_user: "{{ aur_user }}"
